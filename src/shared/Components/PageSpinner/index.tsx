import { Spinner } from "flowbite-react";

function PageSpinner() {
  //const { open } = useSelector((state: any) => state.pageSpinner);
  return <Spinner color="info" aria-label="Info spinner example" />;
}
export default PageSpinner;
